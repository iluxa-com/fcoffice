在jquery中使用ajax请求的并发与非并发实例操作。
注意在使用请求异步时，执行服务端返回成功后的$_this,即原来循环中的元素恒为循环最后的值，所以在这里使用了服务端返回的数据判断(注意get_json1)的写法。比如可以传递一个节点id。同步请求没有这个问题(参考get_json2)。
另外，不同浏览器针对同一域名的默认并发数是不同的。

client.html/client.php是客户端脚本
server.php是服务端脚本。