./configure --user=nginx --group=nginx --prefix=/usr/local/services/nginx --sbin-path=/usr/sbin/nginx --conf-path=/etc/nginx/nginx.conf --with-http_stub_status_module --with-http_ssl_module --lock-path=/var/lock/nginx.lock --pid-path=/var/run/nginx/nginx.pid --with-pcre


./configure --user=nginx --group=nginx \

--prefix=/usr/local/webserver/nginx  \

--sbin-path=/usr/sbin/nginx \

--conf-path=/etc/nginx/nginx.conf \

--with-http_stub_status_module  \

--with-http_ssl_module \

--without-http_rewrite_module \

 --lock-path=/var/lock/nginx.lock \

--pid-path=/var/run/nginx/nginx.pid  \

--with-pcre  //这个一定要加，不加启动nginxd会报错